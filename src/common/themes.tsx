import { createTheme } from "@mui/material/styles";

export const mainTheme = createTheme({
    palette: {
        primary: {
            main: "#69bfbf"
        },
        secondary: {
            main: "#d5e8e8"
        }
    }
});